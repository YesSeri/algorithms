#include <stdio.h>
#include <stdlib.h>


void merge(int *A){

}
int main(int argc, char **argv) {
        int len = *argv[1] - '0';
        printf("arr length: %d\n", len);
        char *charArr = argv[2];
        int j = 0; // arr number
        int k = 0; // place in char arr
        int A[len];
        for (;;) {
        }
        return 0;
}
